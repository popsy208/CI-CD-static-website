name: Deploy CI/CD Website

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Deploy index.html to AWS EC2
      uses: appleboy/scp-action@v0.1.4
      with:
        host: ${{ secrets.HOST }}
        username: hybeeuser
        key: ${{ secrets.SSH_KEY }}
        source: "index.html"
        target: "/home/hybeeuser/myapp"

    - name: Restart Nginx Server
      uses: appleboy/ssh-action@v0.1.6
      with:
        host: ${{ secrets.HOST }}
        username: hybeeuser
        key: ${{ secrets.SSH_KEY }}
        script: |
          sudo cp /home/hybeeuser/myapp/index.html /var/www/html/
          sudo systemctl restart nginx
